---
"@stratakit/mui": patch
---

Added a new `label` prop to `IconButton` and `ToggleButton`. When specified, the value of this prop will be used as the button's accessible name _and_ will also be shown in a tooltip when the button is hovered or focused.
